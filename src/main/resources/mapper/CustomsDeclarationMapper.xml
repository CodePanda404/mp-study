<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mpstudy.mapper.CustomsDeclarationMapper">

    <select id="selectDeclarationPage" resultType="com.example.mpstudy.vo.DeclarationDetailVO">
        SELECT
            d.id,
            d.declaration_no as declarationNo,
            d.status,
            d.total_amount as totalAmount,
            d.currency,
            d.created_at as createdAt,
            -- 联查供应商表
            s.supplier_code AS supplierCode,
            s.supplier_name AS supplierName,
            s.contact_person AS contactPerson,
            -- 联查保税仓库表
            w.warehouse_code AS warehouseCode,
            w.warehouse_name AS warehouseName,
            w.location
        FROM biz_customs_declaration d
        LEFT JOIN sys_supplier s ON d.supplier_id = s.id
        LEFT JOIN sys_warehouse w ON d.warehouse_id = w.id
        <!-- 核心魔法：${ew.customSqlSegment} 会自动解析并替换 Java 端构建的 Where 条件 和 ORDER BY -->
        ${ew.customSqlSegment}
    </select>
</mapper>